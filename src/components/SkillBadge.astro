---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="flex items-center gap-1.5 rounded-full bg-teal-50 px-3 py-1.5 text-sm text-teal-800 ring-1 ring-teal-200">
  <span class="h-1.5 w-1.5 rounded-full bg-teal-500"></span>
  {label}
</span>
